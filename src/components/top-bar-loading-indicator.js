import React from 'react';
import ReactLoading from 'react-loading';

const TopBarLoadingIndicator = () => (
  <ReactLoading color="#fff" delay={0} height={22} type="cylon" width={22} />
);

export default TopBarLoadingIndicator;
